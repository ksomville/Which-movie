<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>HeticRor</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <!-- Google font -->
  <link href='http://fonts.googleapis.com/css?family=Lato' rel='stylesheet' type='text/css'>
  <!-- end Google font -->

  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>


</head>
<body>
<header>
  <%= link_to image_tag("logo.png", :border => 0), root_url, {:class => 'logo'} %>
	<span class="profil">
		<% if user_signed_in? %>
      <div class="round-image-50"><%= link_to image_tag(current_user.avatar.url(:thumb)), '#sidr', :id => 'simple-menu' %></div>
		<% else %>
			<%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
			<%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
		<% end %>

  </span>
</header>
<div id="container">
  <%= yield %>
</div>

<div id="sidr">
  <ul>
    <li><%= link_to 'Profil', edit_user_registration_path, :class => 'navbar-link' %></li>
    <li><%= link_to 'List', '/movies_properties/index' %></li>
    <li><%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link' %></li>
  </ul>
</div>

<footer>
  <p>HETIC 2014 - Jean-Maxime / Kirgan </p>
</footer>
  
  <script>
  $(function() {
    $( "#result_search_container" ).tabs();
  });

$(document).ready(function() {
  $('#simple-menu').sidr({
      side: 'right'
    });
});
  </script>
</body>
</html>
